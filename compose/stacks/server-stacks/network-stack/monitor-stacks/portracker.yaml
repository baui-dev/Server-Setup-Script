# PORTRACKER
# ⚠️  STATUS: NOT FOUND - Docker image and GitHub repository could not be confirmed
# GitHub github.com/portracker/portracker → no such organization found
# Docker Hub image portracker/portracker → does NOT exist
# Note: the original YAML file itself flagged the URL as "(verify URL)" — a sign of uncertainty
#
# ✅ Verified alternatives for port tracking and management:
#   - Portainer:   https://github.com/portainer/portainer         (Docker UI, shows exposed ports)
#                  Image: portainer/portainer-ce
#   - Netdata:     https://github.com/netdata/netdata             (includes port/connection monitoring)
#                  Image: netdata/netdata
#   - Manual:      A config.env file (like this project already uses) is a reliable approach
#   - Beholder:    Search GitHub for "docker port tracker" for community tools
#
# This file is kept as a placeholder. DO NOT deploy as-is — the image does not exist.
# Remove this file or replace with a verified monitoring tool.

# services:
#   portracker:
#     image: portracker/portracker:latest  # ← DOES NOT EXIST
#     container_name: portracker
#     hostname: portracker
#     restart: unless-stopped
#     environment:
#       - TZ=${TZ}
#     volumes:
#       - ${BASE_DATA}/portracker/config:/config
#       - ${BASE_DATA}/portracker/data:/data
#     ports:
#       - ${PORT_PORTRACKER:-8080}:8080
